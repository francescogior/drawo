export type Props = {
  points: Point[],
  drawings: Drawing[],
}

export type UIProps = {
  pointsCounter: number,
  drawingsCounter: number,
  maxMaxX: number,
  avgAvgX: number,
  maxMaxY: number,
  avgAvgY: number,
}
